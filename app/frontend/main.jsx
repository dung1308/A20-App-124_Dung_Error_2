import React from 'react'
import ReactDOM from 'react-dom/client'
import MajorMatchApp from './MajorMatchApp'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MajorMatchApp />
  </React.StrictMode>,
)